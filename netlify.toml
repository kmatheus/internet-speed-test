[build]
  base = "frontend"  # Diretório base onde está o package.json
  command = "npm run build"
  publish = "frontend/build"  # Diretório onde o build será gerado

[[redirects]]
  from = "/speedtest"
  to = "http://localhost:5000/speedtest"
  status = 200
